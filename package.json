{
  "name": "csv-sql",
  "version": "0.0.9",
  "description": "A command line tool for running SQL queries on CSV files.",
  "main": "csv-sql.js",
  "bin": {
    "csv-sql": "./cli.js"
  },
  "scripts": {
    "test": "cd ./build && mocha",
    "build": "babel src --out-dir build",
    "watch": "babel --watch src --out-dir build"
  },
  "keywords": [
    "csv",
    "sql"
  ],
  "author": "David Timms",
  "license": "ISC",
  "repository": "DavidTimms/csv-sql",
  "dependencies": {
    "commander": "^2.9.0",
    "csv": "^0.4.6"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "chai": "^3.4.1",
    "mocha": "^2.3.4"
  }
}
